# Spark Part3 Server

Python server using Claude Agent SDK for conversation-based strength discovery.

## Setup
```bash
pip install claude-agent-sdk fastapi uvicorn
```

## Run
```bash
uvicorn main:app --reload --port 8000
```
